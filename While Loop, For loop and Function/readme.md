# Python Programming: Loops, Control Flow & Functions


## Project Overview

This repository demonstrates my understanding of **core Python programming concepts** through practical, interactive programs.

Rather than focusing only on syntax, the exercises apply Python logic to solve small real-world problems involving **decision-making, repetition, input validation, calculations, and reusable functions**.


This assignment demonstrates fundamental Python programming concepts, including:

* `while` loops
* `for` loops
* `range()` function
* Control statements: `break`, `continue`, and `else`
* User input
* Functions
* Basic mathematical calculations
* Conditional statements

The assignment includes three practical Python programs designed to strengthen understanding of loops, control flow, and functions.


## Objectives

The main objectives of this assignment are to:

* Understand how `while` and `for` loops work.
* Practice using `break` and `continue`.
* Understand the use of `else` with a `while` loop.
* Use the `range()` function with a `for` loop.
* Create and use Python functions.
* Accept and process user input.
* Apply mathematical formulas using Python.



## Programs Included

### 1. Number Guessing Game

A simple number guessing game implemented using a `while` loop.

#### How it works:

* Python randomly generates a number between **1 and 10**.
* The user gets **3 attempts** to guess the number.
* If the user enters a number outside the range 1–10, `continue` is used to request another guess.
* If the correct number is guessed, `break` terminates the loop.
* If all attempts are used without guessing correctly, the `else` block displays a message.

#### Concepts Used

* `while` loop
* `if`, `elif`, `else`
* `break`
* `continue`
* `while...else`
* `random.randint()`
* User input


### 2. Multiplication Table Generator

This program generates a multiplication table from **1 to 10** for a number entered by the user.

#### How it works:

The program:

1. Takes a number from the user.
2. Uses a `for` loop.
3. Uses `range(1, 11)` to generate numbers from 1 through 10.
4. Calculates and displays the multiplication results.

#### Example


Enter the number for which you want the multiplication table: 5

5 x 1 = 5
5 x 2 = 10
5 x 3 = 15
...
5 x 10 = 50


#### Concepts Used

* `for` loop
* `range()`
* User input
* Arithmetic operations
* f-strings


### 3. BMI Calculator

This program calculates Body Mass Index (BMI) using a Python function.

#### Formula Used


BMI = weight (kg) / height (m)²


#### How it works:

* The user enters their weight in kilograms.
* The user enters their height in meters.
* A function called `calculate_bmi()` calculates the BMI.
* The result is rounded to 4 decimal places.

#### Concepts Used

* Functions
* Function parameters
* `return` statement
* User input
* Mathematical calculations
* `round()`



## Technologies Used

* **Python 3**
* **Jupyter Notebook**


## Key Python Concepts Learned

| Concept            | Application                                  |
| ------------------ | -------------------------------------------- |
| `while` loop       | Number Guessing Game                         |
| `for` loop         | Multiplication Table                         |
| `break`            | Exit guessing game when correct              |
| `continue`         | Skip invalid guesses                         |
| `else` with loop   | Display message after attempts are exhausted |
| `range()`          | Generate multiplication table values         |
| Function           | BMI calculation                              |
| Parameters         | Pass weight and height to function           |
| `return`           | Return calculated BMI                        |
| `round()`          | Round BMI result                             |
| `random.randint()` | Generate secret number                       |


# Problem-Solving Approach

The exercises follow a simple problem-solving workflow:

Problem
   ↓
Understand Requirements
   ↓
Identify Inputs
   ↓
Apply Logic
   ↓
Use Loops / Functions
   ↓
Generate Output
   ↓
Validate Result

This approach forms a foundation for writing Python programs used in **data analysis and automation workflows**.

# Key Takeaways

This assignment strengthened my ability to:

* Translate a problem statement into Python logic
* Choose appropriate loops and control statements
* Build reusable functions
* Handle interactive user input
* Perform calculations programmatically
* Write structured and readable Python code

These exercises helped strengthen my understanding of **Python fundamentals, control flow, loops, functions, and problem-solving**.










⭐ *More projects coming as I continue building my data analytics portfolio.*

